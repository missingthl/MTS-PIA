# 阶段二宿主实验

这一层存放当前分类二阶段的宿主实验材料。

当前已经进入这一层的宿主包括：

- `Tensor-CSPNet`
- `ResNet1D`

当前最重要的任务单仍然是：

- [Tensor-CSPNet-端到端局部闭式残差层实现任务单.md](/home/THL/project/MTS-PIA/工程记录/分类/01-阶段二-宿主实验/Tensor-CSPNet-端到端局部闭式残差层实现任务单.md)

阅读顺序建议：

1. 先看 `分类二阶段现状`
2. 再看这里的任务单
3. 再回到 `scripts/hosts/` 与 `models/`
