"""
Local dataset utilities for this repository.

Note:
The package name `datasets` can conflict with the third-party HuggingFace
library (`pip install datasets`). Adding this `__init__.py` ensures Python
imports this local package first when running from the project root.
"""

